# Project Rules

- After completing every step or major change requested by the user, automatically run the appropriate `git add`, `git commit`, and `git push` commands to sync the code to `https://github.com/1tsadityaraj/Real-Estate-Price-Prediction.git`.
